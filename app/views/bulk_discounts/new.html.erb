<%= form_with url: "/merchants/#{@merchant.id}/bulk_discounts", 
              method: :post, local: true do |f| %>
  <%= f.label :quantity %>
  <%= f.text_field :quantity %><br/>
  <%= f.label :discount %>
  <%= f.text_field :discount %><br/>
  <%= f.submit "Submit"%>
<% end %>